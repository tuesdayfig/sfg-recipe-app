# Java Maven CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-java/ for more details
#
version: 2
jobs:
build:
working_directory: ~/sfg-recipe-app
docker:
  # specify the version you desire here
  - image: circleci/openjdk:8-jdk
steps:
  - checkout
  # Download and cache dependencies
  - restore_cache:
paths:
  - ~/.m2
key: sfg-recipe-app-{‌{ checksum "pom.xml" }}
  # run tests!
  - run: mvn package
  - store_test_results:
path: target/sfg-recipe-app-0.0.1-SNAPSHOT.jar